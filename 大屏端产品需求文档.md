# 可视化指挥大屏（ScreenApp）重构产品需求文档 (PRD) v2.0

## 1. 产品概述
### 1.1 背景与现状分析
当前的“农村一标四实系统”可视化指挥大屏 (`ScreenApp.tsx`) 已经具备了基本的框架：全屏天地图底座、左侧统计图表、右侧实时预警列表，以及基于 `setInterval` 的 10 秒级工单数据轮询。
但在深入代码和业务逻辑后，作为产品经理，我发现目前的大屏存在以下几个核心痛点，急需重构：
1. **“假数据”问题**：左侧的 ECharts 图表（人口男女比例、村民外出务工统计）目前完全是**硬编码的静态假数据**，未能真实反映数据库中“实有人口”的动态变化。
2. **“大杂烩”代码架构**：所有的地图配置、图表 Options 声明、UI 布局、数据请求逻辑全部糅合在一个上百行的文件里，组件极度臃肿，后续增加“实有房屋”、“实有单位”等统计维度时将无法维护。
3. **性能隐患**：地图的 Marker 图标在每次数据刷新时都被重复创建，且定时器的简单轮询缺乏请求竞态控制，在大屏长期挂机运行的场景下容易造成内存泄漏或页面卡顿。
4. **适配性差**：左右面板使用了固定的 `400px` 宽度，地图中心点坐标被写死，缺乏针对不同分辨率物理大屏的自适应能力。

### 1.2 重构目标 (V2.0 升级版)
本次重构的核心目标是**“让数据活起来，让架构立起来，让体验飞起来”**：
1. **数据真实化与丰富化**：打通全局 Store，将左侧统计图表的数据源替换为对真实 `populations`、`houses` 数据的实时计算，并增加全局核心指标（KPI）展板。
2. **组件模块化与高可扩展性**：将大屏拆分为高内聚、低耦合的独立组件。为未来走向“组件化配置”打下基础。
3. **深度交互与多维联动**：建立“图-地”、“表-地”联动机制，提升大屏的指挥调度和微观探查能力。
4. **极致性能与商用级适配**：引入 CSS Transform 等比例缩放方案完美适配各类物理大屏；优化轮询机制（或引入 WebSocket）以实现毫秒级响应并杜绝内存泄漏。

---

## 2. 需求详情 (What Changes)

### 2.1 架构与组件拆分需求
为了提升可维护性，需将原有的 `ScreenApp.tsx` 拆分为以下结构：
- `src/pages/screen/ScreenApp.tsx` (主容器，负责 Layout、自适应缩放计算和顶层状态分发)
  - ├── `components/MapLayer.tsx` (纯粹的地图渲染组件，接收坐标和工单状态集，处理地图交互)
  - ├── `components/HeaderPanel.tsx` (顶部全局核心指标数据面板 - **V2.0 新增**)
  - ├── `components/LeftPanel.tsx` (左侧数据面板，负责接收真实人口/资产数据并渲染 ECharts)
  - └── `components/RightPanel.tsx` (右侧预警面板，负责展示异常工单流)

### 2.2 核心指标与图表数据真实化需求
**需求描述**：大屏的数据展现需具备宏观概览与微观分析能力，且必须反映系统的真实状态。
- **全局核心指标（HeaderPanel）**：
  - 顶部展示 4-6 个核心数字 KPI（如：实有人口总数、房屋总数、今日新增预警、未处置隐患数等），引入**数字翻牌器（CountUp）动效**。
- **人口与资产分析图表（LeftPanel）**：
  - **人口结构饼图**：根据 `population` 的不同 `type` 标签（外出务工、留守老人等）进行分组聚合计算占比。
  - **房屋状态柱状图/环形图**：根据 `houses` 的 `status`（自建房、危房、民宿等）进行分布统计。
  - *所有图表入场需有平滑的加载动效，数据刷新时平滑过渡。*

### 2.3 地图交互与多维联动需求 (V2.0 重点)
**需求描述**：提升大屏地图的智能性、渲染性能及指挥调度交互体验。
- **自动居中 (FitBounds)**：
  - 根据当前加载的 `addresses` 列表，动态计算所有点位的经纬度边界，使地图在初始化时能够自动缩放并完美包裹住所有村庄点位。
- **“表-地”联动 (Linkage & Drill-down)**：
  - 当用户在右侧预警列表点击某条紧急工单时，地图需触发平滑飞行动画 (`map.flyTo`) 聚焦至事发坐标，并自动弹出包含该地址及关联人员/房屋详细信息的自定义 Popup 窗口。
- **图标缓存优化**：
  - 将正常的蓝色 Marker 和异常的红色闪烁 Marker 提取为单例，避免重复创建 DOM 节点。

### 2.4 大屏适配与性能容灾需求 (V2.0 重点)
- **等比例自适应缩放**：
  - 废弃简单的全屏和固定 px，采用 **CSS3 Transform 缩放方案**（如基于 `1920*1080` 设计稿基准），确保大屏在任何长宽比的显示器下均保持布局完美，不被挤压或裁剪。
- **状态管理与实时通讯**：
  - 优化数据拉取：初始化时一次性并发拉取全量“一标四实”及配置数据。
  - 优化数据更新：暂时保留 10 秒轮询机制，但必须处理好请求竞态（Race Condition）和组件卸载时的定时器清理；远期规划升级为 WebSocket 服务端主动推送。
- **异常降级 (Graceful Degradation)**：
  - 若某个接口（如图表数据）请求失败，该局部区域应展示友好的“数据加载失败”占位状态，绝不能导致整个大屏崩溃白屏。

---

## 3. 验收标准 (Acceptance Criteria)
1. **代码结构验收**：`ScreenApp.tsx` 核心业务逻辑被拆分到至少 4 个独立的子组件中，主文件仅负责布局与自适应缩放。
2. **图表数据验收**：在 PC 后台新增/修改人口或房屋数据后，刷新大屏，顶部核心指标及左侧的 ECharts 图表数据应当发生对应的真实变化。
3. **地图与交互验收**：大屏初始化能自动计算边界居中显示所有点位；点击右侧预警列表项，地图能正确执行 `flyTo` 飞行定位并弹出气泡。
4. **适配与性能验收**：在不同分辨率（如 4K、1080P、带鱼屏）下测试，大屏内容等比例缩放不乱版；大屏持续运行 30 分钟无明显内存泄漏，轮询更新正常。

---
*文档版本：V2.0*  
*产品经理：AI Agent (Data Screen Expert)*