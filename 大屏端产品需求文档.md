# 可视化指挥大屏（ScreenApp）重构产品需求文档 (PRD)

## 1. 产品概述
### 1.1 背景与现状分析
当前的“农村一标四实系统”可视化指挥大屏 (`ScreenApp.tsx`) 已经具备了基本的框架：全屏天地图底座、左侧统计图表、右侧实时预警列表，以及基于 `setInterval` 的 10 秒级工单数据轮询。
但在深入代码和业务逻辑后，作为产品经理，我发现目前的大屏存在以下几个核心痛点，急需重构：
1. **“假数据”问题**：左侧的 ECharts 图表（人口男女比例、村民外出务工统计）目前完全是**硬编码的静态假数据**，未能真实反映数据库中“实有人口”的动态变化。
2. **“大杂烩”代码架构**：所有的地图配置、图表 Options 声明、UI 布局、数据请求逻辑全部糅合在一个上百行的文件里，组件极度臃肿，后续增加“实有房屋”、“实有单位”等统计维度时将无法维护。
3. **性能隐患**：地图的 Marker 图标在每次数据刷新时都被重复创建，且定时器的简单轮询缺乏请求竞态控制，在大屏长期挂机运行的场景下容易造成内存泄漏或页面卡顿。
4. **适配性差**：左右面板使用了固定的 `400px` 宽度，地图中心点坐标被写死，缺乏针对不同分辨率物理大屏的自适应能力。

### 1.2 重构目标
本次重构的核心目标是**“让数据活起来，让架构立起来”**：
1. **数据真实化**：打通全局 Store，将左侧统计图表的数据源替换为对真实 `populations` 数据的实时计算。
2. **组件模块化**：将大屏拆分为高内聚、低耦合的独立组件（地图层、左侧统计面板、右侧预警面板）。
3. **性能与体验优化**：优化地图 Marker 的渲染性能，实现地图基于真实坐标点集的自动缩放居中（Auto Fit Bounds）。

---

## 2. 需求详情 (What Changes)

### 2.1 架构与组件拆分需求
为了提升可维护性，需将原有的 `ScreenApp.tsx` 拆分为以下结构：
- `src/pages/screen/ScreenApp.tsx` (主容器，负责 Layout 和顶层状态分发)
  - ├── `components/MapLayer.tsx` (纯粹的地图渲染组件，接收坐标和工单状态集)
  - ├── `components/LeftPanel.tsx` (左侧数据面板，负责接收真实人口数据并渲染 ECharts)
  - └── `components/RightPanel.tsx` (右侧预警面板，负责展示异常工单流)

### 2.2 左侧图表数据真实化需求
**需求描述**：左侧面板的图表必须反映系统的真实状态。
- **人口属性分析图（饼图）**：
  - 数据源：`useStore().populations`。
  - 逻辑：根据 `population` 的不同 `type` 标签（如：外出务工、留守老人、退役军人、村干部、留守儿童等）进行分组聚合，计算出每种类型的人数占比。
- **房屋/单位资产分析（新增图表）**：
  - 数据源：`useStore().houses` 和 `useStore().units`。
  - 逻辑：将原来的“务工流向统计”替换或增加为更符合一标四实业务的“房屋状态分布”（如：自建房、危房、民宿）或“实有单位类型分布”，进一步丰富大屏的数据厚度。

### 2.3 地图交互与性能优化需求
**需求描述**：提升大屏地图的智能性和渲染性能。
- **自动居中 (FitBounds)**：
  - 废弃写死的 `center={[23.85, 113.89]}`。
  - 根据当前加载的 `addresses` 列表，动态计算所有点位的经纬度边界（Bounds），使地图在初始化时能够自动缩放并完美包裹住所有村庄点位。
- **图标缓存优化**：
  - 将正常的蓝色 Marker 和异常的红色闪烁 Marker 提取为模块级别的单例，避免在 `.map` 循环中重复执行 `L.divIcon` 的创建操作。

### 2.4 数据拉取与状态管理优化
- 大屏初始化时，需一次性拉取完整的“一标四实”全量数据（`fetchAddresses`, `fetchPopulations`, `fetchHouses`, `fetchUnits`, `fetchOrders`, `fetchConfig`）。
- 保持工单的 10 秒轮询机制，但建议在 `useEffect` 清理函数中确保前一个请求的回调被正确取消，防止内存泄漏。

---

## 3. 验收标准 (Acceptance Criteria)
1. **代码结构验收**：`ScreenApp.tsx` 代码行数大幅减少，核心业务逻辑被拆分到 `components/` 目录下的三个子组件中。
2. **图表数据验收**：在 PC 后台新增一个“留守老人”人口或修改一个房屋状态后，刷新大屏，左侧的 ECharts 图表数据应当发生对应的真实变化。
3. **地图表现验收**：大屏加载完毕后，地图的缩放级别和中心点应自动适配当前数据库中的莲麻村点位群，不出现点位溢出屏幕或点位过于集中的情况。
4. **性能监控验收**：大屏在浏览器中持续运行 30 分钟，内存占用平稳，不出现明显的页面卡顿，右侧预警工单的轮询更新正常。

---
*文档版本：V1.0*  
*产品经理：AI Agent*