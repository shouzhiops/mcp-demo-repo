# 基层治理系统 - 模块完整功能测试用例

## 1. 矛盾纠纷台账 (Disputes)
* **前置条件**：后端服务正常启动，数据库已连通，存在至少 1 名网格员和 1 名群众。
* **TC-1.1 列表渲染**：访问 `/admin/disputes`，表格应正常展示“标题、类型、内容、状态、调解员”等列，并包含已有的测试数据。
* **TC-1.2 新增记录**：
  * 操作：点击“新增”按钮。
  * 输入：标题“测试纠纷”，类型“邻里纠纷”，内容“噪音扰民”，状态“待调解”，涉事人员选择任意存在的人员。
  * 预期：保存成功，表格顶部出现“测试纠纷”记录。
* **TC-1.3 编辑记录**：
  * 操作：点击“测试纠纷”记录的“编辑”按钮。
  * 输入：状态修改为“调解中”。
  * 预期：保存成功，表格中该记录状态更新为“调解中”。
* **TC-1.4 删除记录**：
  * 操作：点击“测试纠纷”记录的“删除”按钮，在气泡确认框中点击“确认”。
  * 预期：删除成功，表格中该记录消失。

## 2. 工程项目台账 (Projects)
* **前置条件**：后端服务正常启动。
* **TC-2.1 列表渲染**：访问 `/admin/projects`，表格应正常展示项目列表。
* **TC-2.2 新增记录**：
  * 操作：点击“新增”按钮。
  * 输入：名称“测试项目”，投资额“100”，面积“50”，进度“前期准备”，困难“无”。
  * 预期：保存成功，表格中出现“测试项目”记录。
* **TC-2.3 编辑记录**：
  * 操作：点击“编辑”，修改进度为“施工中”。
  * 预期：保存成功，进度更新。
* **TC-2.4 删除记录**：
  * 操作：点击“删除”并确认。
  * 预期：删除成功，该记录消失。

## 3. 流动人口台账 (Floating Populations)
* **前置条件**：后端服务正常，存在人口和房屋数据。
* **TC-3.1 列表渲染**：访问 `/admin/floating-populations`，表格应正常展示。
* **TC-3.2 新增记录**：
  * 操作：点击“新增”按钮。
  * 输入：选择人口、选择房屋，来源地“外省”，原因“务工”，到期时间“2025-12-31”。
  * 预期：保存成功，记录展示。
* **TC-3.3 编辑记录**：
  * 操作：点击“编辑”，修改原因为“探亲”。
  * 预期：保存成功，原因更新。
* **TC-3.4 删除记录**：
  * 操作：点击“删除”并确认。
  * 预期：删除成功。

## 4. 房屋巡查台账 (House Inspections)
* **前置条件**：后端服务正常，存在房屋数据。
* **TC-4.1 列表渲染**：访问 `/admin/house-inspections`，表格正常展示。
* **TC-4.2 新增记录**：
  * 操作：点击“新增”按钮。
  * 输入：选择房屋，结构“砖混”，用途“出租”，隐患“电线老化”，状态“未整改”。
  * 预期：保存成功，记录展示。
* **TC-4.3 编辑记录**：
  * 操作：点击“编辑”，修改状态为“已整改”。
  * 预期：保存成功，状态更新。
* **TC-4.4 删除记录**：
  * 操作：点击“删除”并确认。
  * 预期：删除成功。

## 5. 督导任务台账 (Supervision Tasks)
* **前置条件**：后端服务正常。
* **TC-5.1 列表渲染**：访问 `/admin/supervision-tasks`，表格正常展示。
* **TC-5.2 新增记录**：
  * 操作：点击“新增”按钮。
  * 输入：来源“上级交办”，内容“测试督导任务”，截止时间选择明天，状态“进行中”。
  * 预期：保存成功，记录展示。
* **TC-5.3 编辑记录**：
  * 操作：点击“编辑”，修改状态为“已完成”。
  * 预期：保存成功，状态更新。
* **TC-5.4 删除记录**：
  * 操作：点击“删除”并确认。
  * 预期：删除成功。
