# 基层治理系统 - 产品需求文档 (PRD)

## 1. 产品概述
本系统是一套专为基层治理（如镇、村、网格）打造的信息化管理平台，旨在全面整合“人、地、事、物、组织”等基层核心数据。系统主要分为基础台账、业务台账、数据看板与关系图谱四大板块，帮助管理者实现隐患排查、矛盾化解、工程追踪和流动人口管理的闭环流转，全面提升社会治理效能。

---

## 2. 全局通用功能与规范

*   **列表功能规范**：所有模块的列表均需支持**分页**（默认 10 条/页）、**列宽自适应与横向滚动**（针对字段较多的表格）、并默认按照更新时间或创建时间**倒序排序**。
*   **搜索与筛选**：所有列表顶部必须提供**综合模糊搜索**输入框（支持对关键字段如名称、地址、描述的模糊检索）。
*   **增删改查 (CRUD)**：所有业务模块必须具备“新增”、“编辑”、“删除”的完整生命周期管理。删除操作需带有二次确认弹窗。
*   **导出功能**：所有模块支持“导出台账”功能（将当前列表或筛选后的数据导出为 Excel 格式）。
*   **表单验证**：所有必填字段必须带有红色星号提示及前端非空校验，下拉框必须提供占位符。

---

## 3. 基础台账模块

### 3.1 实有人口台账 (Population)
**功能说明**：管理辖区内所有常住及流动人口的基础信息，提供人口多维画像基础。
**扩展功能**：新增人口、编辑人口、删除人口、导出台账。

*   **列表字段**：姓名、身份证号、性别、联系电话、人员类型、关联地址、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索姓名、电话、地址）。
*   **表单字段与枚举值**：
    *   `姓名` (输入框，必填)
    *   `身份证号` (输入框，必填，需符合身份证格式)
    *   `性别` (单选下拉框，必填)：枚举值 `["男", "女"]`
    *   `联系电话` (输入框，选填)
    *   `人员类型` (单选下拉框，必填)：枚举值 `["老人", "儿童", "外出", "干部", "军人", "普通居民"]`
    *   `关联地址` (单选下拉框，支持搜索，必填)：关联至标准地址库的 ID。

### 3.2 实有房屋台账 (House)
**功能说明**：记录辖区内所有建筑物的基本属性及使用状态，重点关注危房及出租房屋。
**扩展功能**：新增房屋、编辑房屋、删除房屋、导出台账。

*   **列表字段**：房屋ID、房屋状态、房屋用途、产权人、产权人电话、关联地址、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索产权人、电话、地址）。
*   **表单字段与枚举值**：
    *   `房屋状态` (单选下拉框，必填)：枚举值 `["自建房", "商品房", "危房", "空心房", "民宿", "农家乐", "其他"]`
    *   `房屋用途` (单选下拉框，必填)：枚举值 `["自住", "出租", "商用", "闲置", "其他"]`
    *   `产权人` (输入框，选填)
    *   `产权人电话` (输入框，选填)
    *   `关联地址` (单选下拉框，支持搜索，必填)：关联至标准地址库的 ID。

### 3.3 实有单位台账 (Unit)
**功能说明**：管理辖区内所有企事业单位、个体工商户等组织机构的信息。
**扩展功能**：新增单位、编辑单位、删除单位、导出台账。

*   **列表字段**：单位ID、单位名称、单位类型、法人/负责人、联系电话、关联地址、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索单位名称、法人、地址）。
*   **表单字段与枚举值**：
    *   `单位名称` (输入框，必填)
    *   `单位类型` (单选下拉框，必填)：枚举值 `["特色餐饮", "住宿服务", "加工制造", "机关企事业", "其他"]`
    *   `法人/负责人` (输入框，选填)
    *   `联系电话` (输入框，选填)
    *   `关联地址` (单选下拉框，支持搜索，必填)：关联至标准地址库的 ID。

### 3.4 实有设施台账 (Facility)
**功能说明**：管理辖区内的公共设施设备状态，如消防栓、监控摄像头等。
**扩展功能**：新增设施、编辑设施、删除设施、导出台账。

*   **列表字段**：设施ID、设施名称、设施类型、运行状态、管护责任人、关联地址、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索设施名称、责任人、地址）。
*   **表单字段与枚举值**：
    *   `设施名称` (输入框，选填)
    *   `设施类型` (单选下拉框，必填)：枚举值 `["消防", "监控", "垃圾", "交通", "照明"]`
    *   `运行状态` (单选下拉框，必填)：枚举值 `["正常", "维护中", "损坏"]`
    *   `管护责任人` (输入框，选填)
    *   `关联地址` (单选下拉框，支持搜索，必填)：关联至标准地址库的 ID。

### 3.5 标准地址台账 (Address)
**功能说明**：统一管理辖区内的标准网格化地址数据，为其他所有台账提供基础空间坐标与地址字典。
**扩展功能**：新增地址、编辑地址、删除地址。

*   **列表字段**：地址ID（如：某某村-某某组-001号）、地址名称、经度、纬度、层级、类型、操作。
*   **搜索/筛选条件**：综合搜索框（支持搜索地址ID、名称）。
*   **表单字段与枚举值**：
    *   `地址ID` (输入框，必填，唯一约束)
    *   `地址名称` (输入框，必填)
    *   `经度` (数字输入框，必填)
    *   `纬度` (数字输入框，必填)
    *   `层级` (单选下拉框，必填)：如 `["村/网格"]` 等
    *   `类型` (单选下拉框，必填)：如 `["住宅"]` 等


---

## 4. 业务台账模块

### 4.1 异常预警工单 (Orders)
**功能说明**：负责接收、分拨、督办和销账由群众上报、物联网告警或网格员巡查发现的各类安全隐患和异常事件。
**扩展功能**：新增工单、编辑工单、删除工单、导出台账、**状态流转（分拨/督办/审核销账）**。

*   **列表字段**：工单编号、隐患类型、隐患来源、优先级、当前状态、详情描述、关联地址、上报时间、操作（状态流转/编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索隐患类型、描述、关联地址、来源）。
*   **表单字段与枚举值**：
    *   `隐患类型` (输入框，必填)
    *   `当前状态` (单选下拉框，必填)：枚举值 `["待分拨", "待处置", "已处置", "已销账"]`
    *   `关联地址` (单选下拉框，支持搜索，必填)：关联至标准地址库的 ID。
    *   `隐患来源` (单选下拉框，选填)：枚举值 `["群众上报", "物联网告警", "网格员巡查"]`
    *   `优先级` (单选下拉框，选填)：枚举值 `["高", "中", "低"]`
    *   `详情描述` (多行文本框，选填)
*   **状态流转操作**：
    *   待分拨 -> 待处置（分拨操作，可选填处理人员ID）
    *   待处置 -> 已处置（督办催办）
    *   已处置 -> 已销账（审核销账）

### 4.2 矛盾纠纷台账 (Disputes)
**功能说明**：记录辖区内发生的各类矛盾纠纷，跟踪调解进度，防范群体性事件或民事转刑事案件。
**扩展功能**：新增纠纷、编辑纠纷、删除纠纷、导出台账。

*   **列表字段**：纠纷ID、标题、类型、内容、状态、调解员ID、创建时间、更新时间、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索标题、类型、内容）。
*   **表单字段与枚举值**：
    *   `标题` (输入框，必填)
    *   `类型` (输入框，必填)
    *   `内容` (多行文本框，必填)
    *   `状态` (单选下拉框，必填)：枚举值 `["待处理", "处理中", "已解决"]`
    *   `调解员ID` (数字输入框，选填)：关联处理人员 ID。

### 4.3 工程项目台账 (Projects)
**功能说明**：管理辖区内的基建、惠民等工程项目，跟踪项目进度及投资情况。
**扩展功能**：新增项目、编辑项目、删除项目、导出台账。

*   **列表字段**：项目ID、项目名称、投资金额(万元)、占地面积(㎡)、进度、困难问题、负责人ID、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索名称、进度、困难问题）。
*   **表单字段与枚举值**：
    *   `项目名称` (输入框，必填)
    *   `投资金额` (数字输入框，必填)
    *   `占地面积` (数字输入框，必填)
    *   `项目进度` (输入框，必填)
    *   `困难问题` (多行文本框，选填)
    *   `负责人ID` (数字输入框，选填)

### 4.4 流动人口台账 (FloatingPopulations)
**功能说明**：针对外来务工、经商等流动人口进行专项登记与管理，掌握人口流入流出动态。
**扩展功能**：新增流动记录、编辑流动记录、删除流动记录、导出台账。

*   **列表字段**：记录ID、流动人口(ID)、现住房屋(ID)、来源地、流入原因、过期时间、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索来源地、流入原因）。
*   **表单字段与枚举值**：
    *   `流动人口` (单选下拉框，必填)：关联 Population 库的人员 ID。
    *   `现住房屋` (单选下拉框，必填)：关联 House 库的房屋 ID。
    *   `来源地` (输入框，必填)
    *   `流入原因` (输入框，必填)
    *   `过期时间` (日期选择器，选填)

### 4.5 房屋巡查台账 (HouseInspections)
**功能说明**：记录网格员对辖区内房屋的定期巡查情况，防范建筑安全隐患及违规使用问题。
**扩展功能**：新增巡查记录、编辑记录、删除记录、导出台账。

*   **列表字段**：巡查ID、巡查房屋(ID)、建筑结构、使用情况、安全隐患、整改期限、状态、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索建筑结构、使用情况、隐患描述）。
*   **表单字段与枚举值**：
    *   `巡查房屋` (单选下拉框，必填)：关联 House 库的房屋 ID。
    *   `建筑结构` (输入框，必填)：如砖木、钢混等。
    *   `使用情况` (输入框，必填)：如自住、出租等。
    *   `安全隐患` (多行文本框，必填)
    *   `整改期限` (日期选择器，选填)
    *   `状态` (单选下拉框，必填)：枚举值 `["正常", "存在隐患", "需整改"]`

### 4.6 督导任务台账 (SupervisionTasks)
**功能说明**：接收和处理上级下达的督导任务，跟踪任务执行情况并提交督导报告。
**扩展功能**：新增任务、编辑任务、删除任务、导出台账。

*   **列表字段**：任务ID、任务来源、任务内容、截止期限、处理人ID、状态、督导报告、操作（编辑/删除）。
*   **搜索/筛选条件**：综合搜索框（支持搜索任务来源、内容、督导报告）。
*   **表单字段与枚举值**：
    *   `任务来源` (输入框，必填)
    *   `任务内容` (多行文本框，必填)
    *   `截止期限` (日期选择器，必填)
    *   `处理人ID` (数字输入框，选填)
    *   `状态` (单选下拉框，必填)：枚举值 `["待分配", "进行中", "已完成"]`
    *   `督导报告` (多行文本框，选填)


---

## 5. 数据看板与智能分析模块

### 5.1 概览仪表盘 (Dashboard)
**功能说明**：系统登录后的默认首页，为管理者提供全局视角的统计概览与快捷操作入口。
**页面功能说明**：
*   **核心指标统计卡片**：实时统计并展示“已登记标准地址”、“实有人口”、“实有房屋”、“实有单位”、“实有设施”、“待处理隐患”的总数。点击卡片可快速跳转至对应台账列表。
*   **快捷入口导航**：提供六大核心模块（标准地址、人口、房屋、单位、设施、隐患分拨）的快捷点击入口。
*   **多端协同预览**：
    *   提供移动端（网格员使用）的实时二维码及跳转链接。
    *   提供指挥大屏（大屏端）的快速访问链接。
*   **最近待办隐患列表**：实时抓取并展示最新的 5 条“待分拨”或“待处置”状态的异常预警工单，展示隐患类型、状态标签、优先级、描述及上报时间，并提供“去处理”按钮一键跳转。

### 5.2 人物关系图谱 (PersonGraph)
**功能说明**：以特定人员为核心，通过可视化力导向图（Force Graph）将人、房、事（纠纷）、地址等原本孤立的底层台账数据进行全方位串联，揭示隐蔽的社会网络。
**页面功能与交互说明**：
*   **图谱渲染与节点样式**：
    *   利用 AntV G6 引擎进行力导向布局渲染，节点带有物理排斥力防重叠。
    *   不同实体采用不同形状与颜色区分：
        *   人员 (Person)：蓝色圆形
        *   房屋 (House)：绿色矩形
        *   纠纷 (Dispute)：红色三角形
        *   地址 (Address)：橙黄色菱形
    *   连线展示明确的关系标签（如：居住、拥有、涉事等）与箭头方向。
*   **动态交互操作**：
    *   **画布漫游**：支持鼠标拖拽平移画布、滚轮缩放大小。
    *   **节点交互**：支持自由拖拽单个节点调整位置。
    *   **详情悬停 (Tooltip)**：鼠标悬停在任意节点上时，自动弹出浮层展示该节点对应的底层台账详细属性（如人员的类型、房屋的状态、纠纷的调解进度等）。

---

## 6. 非功能性需求
1.  **多端适配**：系统前端需具备响应式能力，大屏端侧重数据可视化呈现，管理后台侧重表单与列表操作，移动端侧重工单上报与现场核查。
2.  **性能要求**：人物关系图谱在渲染 500 个节点以内时，帧率需保持流畅；所有列表查询响应时间需控制在 500ms 以内。
3.  **操作日志**：对于数据的“删除”和“状态流转”等高危操作，后台需记录操作日志以便追溯。
