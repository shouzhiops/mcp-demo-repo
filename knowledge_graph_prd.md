# 基层治理系统 - 人物关系图谱需求文档 (PRD)

## 1. 产品概述
### 1.1 背景与目的
人物关系图谱是基层治理系统中不可或缺的情报可视化工具。在基层社会治理与维稳情报分析中，单层静态表格数据无法揭示隐蔽的社会网络和历史演变。
本模块旨在以特定人员为核心，通过“点（实体）”和“边（关系）”的形式，将人、房、事（纠纷）、物、组织等原本孤立的底层台账数据进行全方位串联。通过可视化的力导向布局、多跳下钻、智能预警及深度交互，赋能网格员与管理者进行更精准的风险排查和决策指挥。

### 1.2 目标用户
*   **网格员/基层干部**：用于日常走访前的情报摸底，快速了解重点人员的社会关系、居住轨迹和历史背景。
*   **综治中心指挥员/数据分析员**：用于复杂矛盾纠纷的溯源、群体性事件的隐患排查及人员轨迹分析。

---

## 2. 已实现核心功能 (Current Features)

目前系统已初步实现了基于 `@antv/g6` 的 1 度（直连）关系网络可视化，具体功能如下：

### 2.1 基础数据整合与模型构建
*   **实体节点 (Nodes)**：
    *   **人员 (Person)**：包含被查询的中心人员及其社会关系人员（展示性别、电话等属性）。
    *   **房屋 (House)**：包含人员名下的产权房屋，以及作为流动人口暂住的出租屋（展示状态、用途等属性）。
    *   **地址 (Address)**：人员的常住标准地址。
    *   **纠纷 (Dispute)**：该人员涉事参与的矛盾纠纷记录（展示纠纷类型、处理状态）。
*   **关系连线 (Edges)**：
    *   支持 5 种核心关系的抽取与连线：**“居住在”**（人→地址）、**“流动居住”**（人→房屋）、**“产权人”**（人→房屋）、**“涉事”**（人→纠纷）、**“社会关系”**（人↔人，如父子、夫妻等自定义标签）。

### 2.2 可视化渲染与基础交互
*   **语义化视觉设计**：
    *   通过不同的颜色与形状区分实体：人员（蓝色/圆形）、房屋（绿色/矩形）、纠纷（红色/三角形）、地址（橙黄/菱形）。
    *   连线带有明确的方向箭头及带有背景底色的文字标签。
*   **力导向物理布局 (Force Layout)**：
    *   节点之间带有物理排斥力，自动散开防重叠，呈现以中心人员为圆心的星状发散结构。
*   **悬浮提示与画布操作**：
    *   支持鼠标悬停 (Tooltip) 查看节点的详细业务属性（如人员电话、房屋用途等）。
    *   支持鼠标拖拽画布、滚轮缩放，以及自由拖拽整理单一节点的位置。

---

## 3. 深度迭代需求 (Upcoming Features)

为将图谱从“静态展示工具”升级为“动态情报分析引擎”，规划以下深度迭代需求：

### 3.1 深度数据扩展与多跳支撑 (Data Integration)
*   **FR-1.1 接入工作单位与就业网络**：
    *   建立人员 (`Population`) 与实有单位 (`Unit`) 的“雇佣/法人”关系边。
    *   新增【单位节点】（紫色/六边形）。
*   **FR-1.2 接入隐患工单与督导任务**：
    *   将人员作为反映人或当事人关联至隐患工单 (`Order`) 和督导任务 (`SupervisionTask`)。
    *   新增【工单/任务节点】（橙色/五边形），并在连线上标注人员在事件中的具体角色。
*   **FR-1.3 多跳关系查询 (Multi-hop)**：
    *   后端 API 从现有的 1 度扩展为支持 2 度至 N 度深度查询（如：提取“亲属名下的重点房屋”或“同案人员的关联纠纷”）。

### 3.2 动态交互与深度探索 (Interactive Exploration)
*   **FR-2.1 无限下钻（动态节点生长）**：
    *   用户双击画布上的任意非核心【人员节点】时，前端局部刷新，向后端请求该人员的社会关系网，并将其“生长”叠加至当前画布，实现关系网的无限延伸。
*   **FR-2.2 图例面板与动态过滤**：
    *   在画布边缘悬浮图例面板，展示各类节点（人、房、事）的统计数量。
    *   支持用户通过勾选图例（如取消勾选“房屋”），实时隐藏图谱中对应的节点和连线，降低视觉噪音。
*   **FR-2.3 右键业务操作菜单 (Context Menu)**：
    *   右键点击任意节点，弹出对应业务菜单。
    *   例如，右键人员节点可“查看台账详情”或“新增纠纷”；右键纠纷节点可“指派处理人”或“更新状态”，打通分析与业务的闭环。
*   **FR-2.4 多种布局一键切换**：
    *   提供顶部工具栏，支持在现有的“力导向 (Force)”布局与“同心圆 (Concentric)”、“辐射状 (Radial)”布局间无缝切换。

### 3.3 智能分析与预警可视化 (Analytical Capabilities)
*   **FR-3.1 风险穿透与高亮预警**：
    *   **业务规则**：当关联房屋为“危房/需整改”，或纠纷处于“未化解/升级”状态时触发。
    *   **视觉表现**：对应节点呈现**红色呼吸灯动效**（外圈阴影闪烁），并附加红色“!”警示角标。
*   **FR-3.2 重点人员画像侧边栏 (Profile Dashboard)**：
    *   选中任意人员节点时，右侧滑出抽屉式面板，自动计算并展示摘要标签（如“关联 3 起未化解纠纷”、“名下 2 处群租房”、“频繁流动人口”）。
*   **FR-3.3 隐藏关系探路 (Shortest Path)**：
    *   允许用户在图谱中利用 Ctrl/Cmd 点击选中任意两个节点，点击“关系探路”。
    *   系统使用最短路径算法（如 Dijkstra），高亮显示连接两人的最短关联路径，非相关路径变灰。

### 3.4 时序演变与轨迹回溯 (Time-series Analysis)
*   **FR-4.1 图谱时间轴播放器**：
    *   在画布底部引入带播放按钮的时间轴滑块（按月/年划分）。
    *   拖动滑块时，图谱仅渲染该时间节点之前成立的关系（如过去的居住地、历史纠纷的发生）。
    *   支持点击“播放”，动态演示该人员社会关系网随时间推移的“生长”与“消亡”过程，辅助追踪重点人员轨迹。

---

## 4. 非功能性需求
*   **性能要求**：
    *   节点数量在 500 个、连线 1000 条以内时，前端力导向布局计算和渲染不应出现明显卡顿（帧率保持 30fps 以上）。
    *   双击下钻请求的后端响应时间控制在 500ms 以内。
*   **安全性要求**：
    *   图谱数据的加载必须严格受限于当前操作用户的 RBAC 权限（例如：网格员只能查看本网格内人员的关系图谱，无法越权下钻至其他辖区）。
*   **兼容性要求**：
    *   使用 AntV G6 引擎渲染，需兼容主流现代浏览器（Chrome、Edge、Safari 最新版）。

---

## 5. 迭代实施计划 (Phases)
为确保项目平稳落地，建议分三期进行后续迭代：
*   **Phase 1（数据扩容与基础交互）**：完成单位、工单等新实体的数据接入，实现图例过滤面板与右键操作菜单。
*   **Phase 2（深度下钻与预警分析）**：实现双击节点无限下钻逻辑，开发风险呼吸灯动效与侧边栏人员画像面板。
*   **Phase 3（高级算法引擎）**：引入最短路径寻路算法，研发底层时序图谱播放器。