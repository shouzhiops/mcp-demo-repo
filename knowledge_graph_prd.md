# 基层治理系统 - 人物关系图谱深度迭代需求文档 (PRD)

## 1. 产品概述
### 1.1 背景与目的
在当前版本中，人物关系图谱已初步实现了以特定人员为核心的“1度（直连）”关系可视化，打通了人、房、事（纠纷）的静态关联。然而，在复杂的基层社会治理与维稳情报分析中，单层静态数据无法揭示隐蔽的社会网络和历史演变。
本次深度迭代旨在将图谱从“静态数据展示工具”升级为“动态情报分析引擎”，通过引入多跳下钻、智能预警、时序演变和深度交互，赋能网格员与管理者进行更精准的风险排查和决策指挥。

### 1.2 目标用户
*   **网格员/基层干部**：用于日常走访前的情报摸底，快速了解重点人员的社会关系和历史背景。
*   **综治中心指挥员/数据分析员**：用于复杂矛盾纠纷的溯源、群体性事件的隐患排查及人员轨迹分析。

## 2. 核心功能需求

### 2.1 深度数据整合与扩展 (Data Integration)
为全面刻画人员画像，需将系统中现有的底层台账进一步接入图谱。
*   **FR-1.1 接入工作单位与就业网络**：
    *   建立人员 (`Population`) 与实有单位 (`Unit`) 的“雇佣/法人”关系边。
    *   在图谱中新增【单位节点】（紫色/六边形）。
*   **FR-1.2 接入隐患工单与督导任务**：
    *   将人员作为反映人或当事人关联至隐患工单 (`Order`) 和督导任务 (`SupervisionTask`)。
    *   新增【工单节点】（橙色/菱形），并在连线上标注人员在事件中的具体角色。
*   **FR-1.3 多跳关系支撑 (Multi-hop)**：
    *   后端 API 从仅支持 1 度查询扩展为支持 2 度至 N 度关系深度查询，允许提取如“亲属名下的重点房屋”或“同案人员的关联纠纷”。

### 2.2 动态交互与深度探索 (Interactive Exploration)
提升图谱的可玩性与实用性，打破死图限制。
*   **FR-2.1 无限下钻（动态节点扩展）**：
    *   用户双击画布上的任意非核心【人员节点】时，前端触发局部刷新，向后端请求该人员的关系网，并将其“生长”叠加至当前画布，实现关系网的无限延伸。
*   **FR-2.2 图例面板与动态过滤**：
    *   在画布左下角悬浮图例面板，展示各类节点（人、房、纠纷、单位）的统计数量。
    *   支持用户通过勾选图例（如取消勾选“房屋”），实时隐藏图谱中对应的节点和连线，以降低视觉噪音。
*   **FR-2.3 右键业务操作菜单 (Context Menu)**：
    *   右键点击任意节点，弹出对应的业务菜单。
    *   **人员节点**：查看台账详情、新增纠纷记录。
    *   **纠纷/工单节点**：快速指派处理人、更新状态、闭环归档。
*   **FR-2.4 布局一键切换**：
    *   提供顶部工具栏，支持在“力导向 (Force)”、“同心圆 (Concentric)”和“辐射状 (Radial)”布局间一键无缝切换。

### 2.3 智能分析与预警可视化 (Analytical Capabilities)
引入计算模型，将隐患直接暴露给用户。
*   **FR-3.1 风险穿透与高亮预警**：
    *   **业务规则**：当关联房屋状态为“危房/需整改”，或纠纷状态为“未化解/升级”时，触发预警。
    *   **视觉表现**：对应的节点在画布中呈现**红色呼吸灯动效**（外圈阴影闪烁），并在右上角附加红色的“!”警示角标。
*   **FR-3.2 重点人员画像摘要 (Profile Dashboard)**：
    *   选中任意人员节点时，右侧滑出抽屉式情报面板。
    *   自动计算并展示标签：如“关联 3 起未化解纠纷”、“名下 2 处群租房”、“频繁流动人口（近一年换租 3 次）”。
*   **FR-3.3 隐藏关系探路 (Shortest Path)**：
    *   允许用户在图谱中利用 Ctrl/Cmd 点击选中任意两个人员节点，点击“关系探路”。
    *   系统使用最短路径算法（如 Dijkstra），在复杂的网络中高亮显示连接两人的最短关联路径，将非相关路径变灰。

### 2.4 时序演变与轨迹回溯 (Time-series Analysis)
*   **FR-4.1 图谱时间轴播放器**：
    *   在画布底部引入带播放按钮的时间轴滑块（按月/年划分）。
    *   拖动滑块时，图谱仅渲染该时间节点之前成立的关系（如过去的居住地、历史纠纷的发生）。
    *   支持点击“播放”，动态演示该人员的社会关系网随时间推移的“生长”与“消亡”过程，辅助追踪重点人员的流动轨迹。

## 3. 非功能性需求
*   **性能要求**：
    *   节点数量在 500 个、连线 1000 条以内时，前端力导向布局计算和渲染不应出现明显卡顿（帧率保持 30fps 以上）。
    *   双击下钻请求的后端响应时间控制在 500ms 以内。
*   **安全性要求**：
    *   图谱数据的加载必须严格受限于当前操作用户的 RBAC 权限（例如：网格员只能查看本网格内人员的关系图谱，无法越权下钻至其他辖区）。
*   **兼容性要求**：
    *   使用 AntV G6 v5 引擎渲染，需兼容主流现代浏览器（Chrome、Edge、Safari 最新版）。

## 4. 迭代实施计划 (Phases)
为确保项目平稳落地，建议分三期进行实施：
*   **Phase 1（数据扩容与基础交互）**：完成单位、工单等新实体的数据接入，实现图例过滤与右键操作菜单。
*   **Phase 2（深度下钻与预警）**：实现双击无限下钻逻辑，开发风险呼吸灯动效与侧边栏人员画像面板。
*   **Phase 3（高级分析引擎）**：引入最短路径寻路算法，研发底层时序图谱播放器。