# 人物关系图谱模块实施方案 (基于 AntV G6)

## 1. 建设背景与目标
当前“一标四实”系统已具备扎实的实体数据（人口、房屋、纠纷等），但数据呈扁平化分布。为了提升基层综治干部的研判能力，尤其是在处理群体性纠纷、摸排高危人员社会网络时，急需将现有的“孤立表格”升级为“立体关系网络”。
本方案旨在基于现有的 Prisma 数据底座，利用 **AntV G6** 可视化引擎，构建一个可交互、支持下钻、节点动态计算的“人物关系图谱（Knowledge Graph）”模块。

---

## 2. 数据模型层设计 (Prisma)
为了支持显性的社会关系推导，需在现有的 `schema.prisma` 中补充“人与人”的关系表，同时复用已有的“人-事”、“人-房”关联。

### 2.1 新增模型
```prisma
// 新增：人口社会关系表 (显性人际网络)
model PopulationRelation {
  id          Int        @id @default(autoincrement())
  sourceId    Int        // 发起方人口ID (关联 Population)
  targetId    Int        // 接收方人口ID (关联 Population)
  relation    String     // 关系类型：配偶、父子、亲属、债权人、同案犯等
  description String?    // 关系备注
  createdAt   DateTime   @default(now())

  // 外键关联
  source      Population @relation("SourceRelation", fields: [sourceId], references: [id])
  target      Population @relation("TargetRelation", fields: [targetId], references: [id])

  @@unique([sourceId, targetId]) // 防止重复关系
}
```

### 2.2 复用现有隐性关系
*   **同住关系**：通过 `Population` 表中相同的 `addressId` 推导。
*   **涉事关系**：复用现有的 `DisputeRecord` 与 `Population` 的多对多关联（`_DisputeRecordToPopulation`）。
*   **产权/居住关系**：复用现有的 `FloatingRecord` (租客) 以及 `House` 与 `Address` 的绑定。

---

## 3. 后端 API 聚合层设计 (Express)
图谱数据的核心在于**以某一中心节点向外辐射 N 度关系**，避免全表扫描导致前端卡死。

### 3.1 核心接口定义
**`GET /api/graph/person/:id`**
*   **功能**：以指定的人口 ID 为锚点，查询其 1-2 度的关系网络。
*   **请求参数**：
    *   `degree` (可选，默认 1)：向外辐射的层级深度。
*   **响应结构 (标准的 G6 Graph Data 格式)**：
    ```json
    {
      "nodes": [
        { "id": "pop_1", "label": "张三", "group": "person", "properties": { "type": "刑释人员", "risk": "high" } },
        { "id": "house_10", "label": "莲麻村1号", "group": "house", "properties": { "usage": "自住" } },
        { "id": "event_5", "label": "宅基地纠纷", "group": "event", "properties": { "status": "调解中" } }
      ],
      "edges": [
        { "source": "pop_1", "target": "pop_2", "label": "父子" },
        { "source": "pop_1", "target": "house_10", "label": "居住" },
        { "source": "pop_1", "target": "event_5", "label": "涉事方" }
      ]
    }
    ```

### 3.2 聚合算法逻辑
1.  **查询中心节点**：查出目标 Population 的基本信息。
2.  **一度关系辐射 (广度优先)**：
    *   查 `PopulationRelation` 表，获取直系亲属/社会关系。
    *   查 `Address` 下的其他 Population，构建“同住人”连线。
    *   查 `DisputeRecord` 多对多关联表，构建“涉事”连线。
3.  **节点全局去重**：使用 `Map` 或 `Set` 保证返回的 `nodes` 中 ID 唯一（建议节点 ID 采用前缀拼接，如 `pop_1`, `house_10`，防止不同表的主键冲突）。

---

## 4. 前端可视化层设计 (React + AntV G6)
前端在 `/src/pages/admin/` 下新增 `PersonGraph.tsx` 组件，引入 `@antv/g6` 库。

### 4.1 图谱布局与样式配置
*   **布局算法 (Layout)**：采用 G6 的 **Force (力导向布局)** 或 **Concentric (同心圆布局)**。力导向布局能最直观地反映人员聚集的紧密程度（连线越多的核心人物会自动聚拢在中心）。
*   **节点视觉 (Node Design)**：
    *   **人员节点 (Person)**：圆形。根据风险等级（高危/低危）动态改变填充色（红/蓝）。内部可绘制头像 icon。
    *   **事件节点 (Event)**：菱形或三角形。采用警示黄或灰色。
    *   **房屋/地址节点 (House)**：方形。采用绿色。
*   **边视觉 (Edge Design)**：
    *   带箭头的实线/虚线，线中央展示 `label`（如“配偶”、“同案”、“涉事”）。
    *   高危关系（如“同案犯”）使用红色粗线强调。

### 4.2 核心交互功能 (Behaviors)
*   **基础交互**：支持鼠标滚轮缩放（`zoom-canvas`）、拖拽画布（`drag-canvas`）、拖拽节点（`drag-node`）。
*   **节点悬浮 (Tooltip)**：鼠标悬停在节点上时，弹出自定义 HTML 气泡，展示该人员的详细身份证号、近期动态，或该案件的处理进度。
*   **点击高亮连通链路 (Highlight)**：点击某个节点时，自动高亮该节点及其一度关联的所有邻居节点与连线，将无关网络变暗（透明度降低）。
*   **动态双击下钻 (Expand)**：双击边缘的某个“人员 B”节点时，触发二次 API 请求（`GET /api/graph/person/B`），将查出的新节点和边动态 `graph.changeData()` 增量合并到当前画布中，实现图谱的无限延伸探索。

---

## 5. 落地开发步骤规划

| 阶段 | 任务描述 | 责任域 | 耗时预估 |
| :--- | :--- | :--- | :--- |
| **Phase 1** | 更新 `schema.prisma` 新增 `PopulationRelation` 表并执行数据迁移。 | Backend | 0.5 天 |
| **Phase 2** | 在 Express 中开发聚合接口 `/api/graph/person/:id`，实现组装 nodes 和 edges 的组装去重逻辑。 | Backend | 1.5 天 |
| **Phase 3** | 前端安装 `@antv/g6`，封装基础的 `GraphCanvas` React 组件，跑通测试假数据。 | Frontend | 1 天 |
| **Phase 4** | 联调真实接口，实现力导向布局、定制化节点样式（红蓝黄分类）及 Tooltip 悬浮提示。 | Frontend | 1.5 天 |
| **Phase 5** | 实现高级交互：点击节点高亮邻居网络、双击节点动态追加渲染（下钻探索）。 | Frontend | 1 天 |

## 6. 预期业务价值
该模块上线后，综治干部只需在人口台账中点击某高危人员的“查看图谱”按钮，即可瞬间看清该人员“住在哪里、和谁住一起、有什么亲属背景、牵扯过几起矛盾纠纷”。这将彻底改变基层摸排“靠脑子记、靠表格查”的低效现状，实现情报研判的可视化与智能化。