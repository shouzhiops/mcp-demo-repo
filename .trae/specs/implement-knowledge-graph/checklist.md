- [x] `PopulationRelation` 表已成功创建并同步至数据库。
- [x] `GET /api/graph/person/:id` 接口能够正确返回结构化的 `nodes` 和 `edges` JSON 数据，且能辐射到人、房、纠纷。
- [x] 前端成功引入 `@antv/g6`，并且 `PersonGraph.tsx` 组件能渲染出力导向布局的图谱。
- [x] 不同类型（人、房、事）的节点在图谱中能够通过视觉特征（如不同颜色、不同形状的节点）进行明显区分。
- [x] 在管理后台的人口列表页面，能够通过“查看图谱”按钮成功进入某个人物关系图谱页面，且能够正常交互。