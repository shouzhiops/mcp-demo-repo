- [x] 数据库种子数据（`api/prisma/seed.ts`）已全部替换为“广州市从化区莲麻村”及真实的经纬度坐标 `[23.85, 113.89]`。
- [x] 后端 API 能够正确响应 `/api/houses`, `/api/units`, `/api/facilities` 的 GET 请求并携带关联地址数据。
- [x] 前端 `Zustand` 状态管理已添加 `houses`, `units`, `facilities`，应用启动时会调用对应的 `fetch` 方法拉取数据。
- [x] PC 管理后台（`/admin`）的侧边导航栏已包含“实有房屋台账”、“实有单位台账”、“实有设施台账”菜单。
- [x] PC 端新增的“三实”页面能正确展示基于莲麻村的真实模拟数据。
- [x] 可视化指挥大屏（`/screen`）的地图默认中心点已更新为莲麻村的坐标，且预警点能正确显示在地图上。