- [x] 后端 `api` 目录及环境是否正确初始化，依赖是否安装。
- [x] `schema.prisma` 是否已定义核心数据模型，且通过 `npx prisma db push` 创建数据库。
- [x] 数据库 Seed 脚本是否执行成功，包含原始模拟的地址、人口、工单数据。
- [x] Express API 服务（GET, POST, PATCH）是否能正常启动并响应请求（支持 CORS）。
- [x] 前端应用是否已安装 `axios` 或同类工具，并且 `Zustand` 商店中的数据读取已变更为异步请求。
- [x] 移动端上报 -> PC端分拨 -> 移动端处置 -> PC端销账 -> 大屏解除警报 的完整业务闭环是否能在全栈真实架构下跑通。