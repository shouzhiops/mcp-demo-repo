- [x] `schema.prisma` 中已成功建立 `User` 和 `Role` 模型。
- [x] 数据库中存在默认用户 `admin` 且密码验证为 `admin123`。
- [x] 后端提供了注册、登录及用户、角色的 CRUD 接口。
- [x] 未登录状态下访问后台（`/admin`）或移动端（`/mobile`）会被重定向到 `/login`。
- [x] 注册新用户后，登录会提示“账号审核中”。
- [x] 超管登录后，左侧菜单出现“村委班子”和“权限管理”。
- [x] 在“村委班子”中可以审核通过新注册的用户，并在“权限管理”中可以增删改角色。