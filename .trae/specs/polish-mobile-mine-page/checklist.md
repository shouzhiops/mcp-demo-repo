- [x] `Mine.tsx` 的用户信息（头像、昵称、角色）由状态驱动渲染，不再直接硬编码在 JSX 中。
- [x] 列表菜单通过数组 `map` 渲染，点击“消息通知”等菜单项会弹出“功能开发中”的 Toast 提示。
- [x] 移除了多余的自定义箭头图标，使用了组件库原生的 `arrow` 属性。
- [x] 退出登录确认后，执行了清理本地存储的逻辑，并通过 `replace` 模式跳转。
- [x] 页面底部针对全面屏（iPhone 小黑条）增加了安全区适配。