- [x] 组件目录结构已创建，主容器 `ScreenApp.tsx` 的代码行数大幅减少且逻辑清晰。
- [x] 采用 CSS3 Transform 的等比例自适应缩放方案，大屏内容在 `1920x1080` 比例下完美展示，不因窗口调整而形变或错乱。
- [x] 新增 `HeaderPanel` 并在顶部展示基于真实数据的全局核心指标（总人口、总房屋等）。
- [x] `LeftPanel` 中的 ECharts 图表（人口标签占比、房屋状态分布）数据源来自真实的 Store 数据聚合，而非写死的假数据。
- [x] `MapLayer` 在初始化时能够自动计算 `addresses` 的经纬度边界，自适应居中缩放。
- [x] `RightPanel` 与 `MapLayer` 实现了图地联动：点击右侧列表预警项，地图视角平滑拉近 (`flyTo`) 至对应坐标点并弹出详情气泡。
- [x] 确保未配置天地图 Key 时的提示遮罩仍正常工作。