# 人物关系图谱功能测试用例 (PersonGraph Test Cases)

## 模块介绍
测试目标：管理后台“人物关系图谱”模块
被测页面路径：`/admin/population/graph/:id`
前置条件：系统已存在基础的人口、房屋、地址等台账数据。

---

## 测试用例列表

### TC01: 基础渲染与入口测试
- **操作步骤**：
  1. 登录系统，进入“实有人口台账”列表页。
  2. 找到任意一条人员数据，点击操作列的“查看图谱”按钮。
  3. 观察页面跳转及渲染情况。
- **预期结果**：
  1. 成功跳转至 `/admin/population/graph/:id` 页面。
  2. 画布中央加载出以该人员为核心的关系图谱，节点显示正常（圆形、矩形等形状符合图例）。
  3. 左上角显示“人员关系图谱”标题。

### TC02: 图例过滤器测试 (Legend Filter)
- **操作步骤**：
  1. 在图谱页面，找到页面上方的“图例过滤”面板。
  2. 取消勾选“房屋”复选框。
  3. 重新勾选“房屋”复选框。
- **预期结果**：
  1. 取消勾选后，画布中所有的房屋（绿色矩形）节点消失（隐藏）。
  2. 重新勾选后，所有的房屋节点重新在画布中显示。

### TC03: 布局切换测试 (Layout Switcher)
- **操作步骤**：
  1. 在图谱页面顶部，点击“布局方式”下拉框。
  2. 将布局从默认的“力导向 (Force)”切换为“同心圆 (Concentric)”。
  3. 观察画布节点的重新排列过程。
- **预期结果**：
  1. 画布中的节点位置发生平滑的重排，最终呈现为同心圆环状分布。

### TC04: 图谱内搜索测试 (In-graph Search)
- **操作步骤**：
  1. 在顶部搜索框中输入当前中心人员的名字（或存在的某房屋名称，如“房屋”）。
  2. 按下回车键或点击搜索图标。
  3. 点击“清除探路/检索”按钮。
- **预期结果**：
  1. 搜索后，匹配到关键词的节点呈现高亮（蓝色粗边框并带有阴影），其余不相关的节点及连线变暗（灰显，透明度降低）。
  2. 点击清除后，所有节点及连线恢复初始的高亮/正常显示状态。

### TC05: 多跳扩展测试 (Multi-hop Double Click)
- **操作步骤**：
  1. 在画布中找到任意一个非中心人员的节点（如某个房屋或地址节点）。
  2. 使用鼠标双击该节点。
  3. 观察右上角的消息提示与画布变化。
- **预期结果**：
  1. 页面右上角弹出提示框，如“成功扩展 X 个节点，Y 条连线”或“没有更多关联数据”。
  2. 若有新数据，新节点将自动生成并用连线连接至被双击的节点上。

### TC06: 右键上下文菜单与业务闭环测试 (Context Menu)
- **操作步骤**：
  1. 在画布中找到中心人员节点（蓝色圆形），使用鼠标右键点击。
  2. 在弹出的菜单中点击“新增纠纷记录”。
  3. 在弹出的表单中填写纠纷标题（如“测试纠纷”）、类型、详细描述，并点击“确认新增”。
  4. 再次右键点击某节点，选择“查看详情”或类似跳转菜单项。
- **预期结果**：
  1. 右键人员节点时，能够成功弹出包含“新增纠纷记录”、“查看人员详情”的菜单。
  2. 提交表单后，提示“纠纷记录新增成功”，弹窗关闭，且图谱中立刻自动生成一个红色的“纠纷”节点，并连线到该人员。
  3. 点击跳转菜单后，页面跳转至对应的业务台账列表。

### TC07: 关系探路测试 (Shortest Path)
- **操作步骤**：
  1. 按住键盘上的 `Ctrl`（或 Mac 上的 `Cmd`）键。
  2. 用鼠标依次点击画布中不直接相连的两个节点（如节点 A 和节点 B）。
  3. 点击顶部控制栏的“关系探路”按钮。
  4. 点击“清除探路/检索”按钮。
- **预期结果**：
  1. 选中时，被点击的两个节点呈现红色的高亮状态（红色边框及阴影）。
  2. 点击探路后，系统计算并高亮连接这两点的最短路径（相关节点与连线保持高亮），其余不相关的元素全部变暗（灰显）。
  3. 点击清除后，图谱恢复初始状态。
